# additional_tests

Each script in `statistics/` is self-contained and can be run
independently; none depends on another script in this folder.

See `ROADMAP.md` for what each robustness angle tests, and why there's no
separate noise/perturbation subfolder here.
