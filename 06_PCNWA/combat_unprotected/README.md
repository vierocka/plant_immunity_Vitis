# combat_unprotected

Run: `scripts/GCNA_network_analysis.R` (self-contained; reads
`data_files/Rlogs.csv`, writes `Modules_info.csv` and the PCA figure).

See `ROADMAP.md` for why this build is kept alongside the protected one.
